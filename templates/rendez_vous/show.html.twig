{% extends 'base.html.twig' %}

{% block title %}RendezVous{% endblock %}

{% block body %}
    <h1>RendezVous</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id_rendezvous</th>
                <td>{{ rendez_vou.idRendezvous }}</td>
            </tr>
            <tr>
                <th>Date</th>
                <td>{{ rendez_vou.Date ? rendez_vou.Date|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Lieu</th>
                <td>{{ rendez_vou.Lieu }}</td>
            </tr>
            <tr>
                <th>Objective</th>
                <td>{{ rendez_vou.Objective }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_rendez_vous_index') }}">back to list</a>

    <a href="{{ path('app_rendez_vous_edit', {'id_rendezvous': rendez_vou.id_rendezvous}) }}">edit</a>

    {{ include('rendez_vous/_delete_form.html.twig') }}
{% endblock %}
