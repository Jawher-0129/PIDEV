<form method="post" action="{{ path('app_rendez_vous_delete', {'id_rendezvous': rendez_vou.id_rendezvous}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ rendez_vou.id_rendezvous) }}">
    <button class="btn">Delete</button>
</form>
