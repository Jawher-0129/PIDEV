{% extends 'base.html.twig' %}

{% block title %}ModifierDemande{% endblock %}

{% block body %}
<div class="container-fluid bg-primary my-5 py-5">
        <div class="container py-5">
        <h1>Modifier votre Demande</h1>
        </div>
                </div>
    {{ include('demande/_form.html.twig', {'button_label': 'Modifier'}) }}

    <a href="{{ path('app_demande_index') }}">Retour à liste</a>

    {# {{ include('demande/_delete_form.html.twig') }} #}
{% endblock %}
