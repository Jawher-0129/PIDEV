<form method="post" action="{{ path('app_chambre_delete', {'Numero': chambre.Numero}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ chambre.Numero) }}">
    <button class="btn">Supprimer</button>
</form>