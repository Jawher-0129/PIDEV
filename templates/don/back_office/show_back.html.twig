{% extends 'back.html.twig' %}

{% block title %}Don{% endblock %}

{% block body %}
   <div style="text-align: center;">
    <h1 class="mb-4">Don Détail</h1>
</div>


    <div class="card">
        <div class="card-body">
            <table class="table">
                <tbody>
                    <tr>
                        <th scope="row">Id</th>
                        <td>{{ don.id }}</td>
                    </tr>
                    <tr>
                        <th scope="row">Type</th>
                        <td>{{ don.Type }}</td>
                    </tr>
                    <tr>
                        <th scope="row">Date Remise</th>
                        <td>{{ don.DateRemise ? don.DateRemise|date('Y-m-d') : '' }}</td>
                    </tr>
                                   {% if don.Montant %}
                                    <tr>
                                        <th scope="row">Montant</th>
                                        <td>{{ don.Montant }}</td>
                                    </tr>
                                    {% endif %}
                                    {% if don.campagne %}
                                        <tr>
                                            <th scope="row">Campagne</th>
                                            <td>{{ don.campagne.Titre }}</td>
                                        </tr>
                                    {% endif %}
                </tbody>
            </table>
        </div>
    </div>

    <div class="mt-3">
        <a href="{{ path('app_don_index_back') }}" class="btn btn-secondary">Retour</a>
        <a href="{{ path('editDon_back', {'id': don.id}) }}" class="btn btn-primary">Modifier</a>
    </div>

    {{ include('don/back_office/_delete_form_back.html.twig') }}
{% endblock %}

