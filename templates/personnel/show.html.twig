{% extends 'back.html.twig' %}

{% block title %}Personnel{% endblock %}

{% block body %}
    <h1>Personnel</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id_personnel</th>
                <td>{{ personnel.idPersonnel }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ personnel.Nom }}</td>
            </tr>
            <tr>
                <th>Prenom_personnel</th>
                <td>{{ personnel.PrenomPersonnel }}</td>
            </tr>
            <tr>
                <th>Disponibilite</th>
                <td>{{ personnel.Disponibilite ? 'OUI' : 'NON' }}</td>
            </tr>
            <tr>
                <th>Role</th>
                <td>{{ personnel.Role }}</td>
            </tr>
            <tr> {# Add this row for the rating field #}
                <th>Rating</th>
                <td>{{ personnel.rating }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_personnel_index') }}">Retourner à la liste</a>

    <a href="{{ path('app_personnel_edit', {'id_personnel': personnel.idPersonnel}) }}">Modifier</a>

    {{ include('personnel/_delete_form.html.twig') }}
{% endblock %}